#!/bin/sh
# A hacky script to change ".mozilla" directory location
#  remember to have this script before the actual firefox in PATH
echo "This is a firefox fix"
HOME="$XDG_DATA_HOME/firefox" /usr/bin/firefox "$@"
